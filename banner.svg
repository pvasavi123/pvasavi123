<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 800 220" width="100%" height="220" style="background: #0b0f19; border-radius: 12px; font-family: 'Inter', system-ui, sans-serif;">
    <defs>
        <!-- Gradients -->
        <linearGradient id="textGrad" x1="0%" y1="0%" x2="100%" y2="0%">
            <stop offset="0%" stop-color="#60a5fa" />
            <stop offset="50%" stop-color="#818cf8" />
            <stop offset="100%" stop-color="#c084fc" />
        </linearGradient>
        <linearGradient id="borderGrad" x1="0%" y1="0%" x2="100%" y2="100%">
            <stop offset="0%" stop-color="#1e3a8a" stop-opacity="0.8"/>
            <stop offset="100%" stop-color="#581c87" stop-opacity="0.8"/>
        </linearGradient>
        <filter id="glow" x="-20%" y="-20%" width="140%" height="140%">
            <feGaussianBlur stdDeviation="6" result="blur" />
            <feComposite in="SourceGraphic" in2="blur" operator="over" />
        </filter>
    </defs>

    <style>
        /* CSS Animations */
        @keyframes floatY {
            0% { transform: translateY(0px); }
            50% { transform: translateY(-10px); }
            100% { transform: translateY(0px); }
        }
        @keyframes floatReverse {
            0% { transform: translateY(0px); }
            50% { transform: translateY(8px); }
            100% { transform: translateY(0px); }
        }
        @keyframes glowPulse {
            0% { opacity: 0.3; }
            50% { opacity: 0.6; }
            100% { opacity: 0.3; }
        }

        .title-text {
            font-size: 34px;
            font-weight: 800;
            fill: url(#textGrad);
            letter-spacing: 3px;
        }
        .code-accent {
            font-family: 'Fira Code', monospace;
            fill: #3b82f6;
            font-size: 26px;
            font-weight: bold;
            opacity: 0.8;
        }
        .subtitle-text {
            font-size: 16px;
            fill: #9ca3af;
            font-weight: 500;
        }

        /* Floating elements classes */
        .float-node-1 {
            animation: floatY 5s ease-in-out infinite;
        }
        .float-node-2 {
            animation: floatReverse 6s ease-in-out infinite;
        }
        .float-node-3 {
            animation: floatY 4.2s ease-in-out infinite;
        }
        
        .badge-bg {
            fill: #111827;
            stroke: #1f2937;
            stroke-width: 1px;
        }
        .badge-text {
            font-size: 12px;
            font-weight: 600;
            fill: #f3f4f6;
        }
    </style>

    <!-- Animated border glow -->
    <rect x="2" y="2" width="796" height="216" rx="12" fill="none" stroke="url(#borderGrad)" stroke-width="3" />
    <rect x="2" y="2" width="796" height="216" rx="12" fill="none" stroke="#6366f1" stroke-width="3" opacity="0.4" style="animation: glowPulse 4s infinite;" filter="url(#glow)" />

    <!-- Coding bracket icons on edges -->
    <text x="50" y="125" class="code-accent">&lt;</text>
    <text x="730" y="125" class="code-accent">/&gt;</text>

    <!-- Center Profile Info -->
    <g transform="translate(400, 105)" text-anchor="middle">
        <text y="-25" class="title-text">VASAVI VARMA</text>
        <text y="15" class="subtitle-text">Full Stack Developer | Java &amp; Spring Boot | React | Django</text>
        <text y="45" font-size="12" fill="#6b7280" font-weight="600" letter-spacing="1.5">BUILD. LEARN. IMPROVE. REPEAT.</text>
    </g>

    <!-- Floating badges (using nesting to isolate static translation from CSS float) -->
    <!-- Node 1: Java (Top Left) -->
    <g transform="translate(100, 40)">
        <g class="float-node-1">
            <rect class="badge-bg" width="70" height="28" rx="8"/>
            <text x="35" y="18" text-anchor="middle" class="badge-text" fill="#f59e0b">Java ☕</text>
        </g>
    </g>

    <!-- Node 2: React (Bottom Left) -->
    <g transform="translate(130, 145)">
        <g class="float-node-2">
            <rect class="badge-bg" width="80" height="28" rx="8"/>
            <text x="40" y="18" text-anchor="middle" class="badge-text" fill="#06b6d4">React ⚛️</text>
        </g>
    </g>

    <!-- Node 3: Spring Boot (Top Right) -->
    <g transform="translate(620, 40)">
        <g class="float-node-3">
            <rect class="badge-bg" width="95" height="28" rx="8"/>
            <text x="47.5" y="18" text-anchor="middle" class="badge-text" fill="#10b981">Spring Boot 🍃</text>
        </g>
    </g>

    <!-- Node 4: Django (Bottom Right) -->
    <g transform="translate(580, 145)">
        <g class="float-node-2">
            <rect class="badge-bg" width="85" height="28" rx="8"/>
            <text x="42.5" y="18" text-anchor="middle" class="badge-text" fill="#047857">Django 🐍</text>
        </g>
    </g>
</svg>
